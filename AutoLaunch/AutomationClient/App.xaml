<Application x:Class="AutomationClient.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="Designer.xaml">
    <Application.Resources>
        <!--
            <Style x:Key="DataGridColumnStyle" TargetType="DataGridColumnHeader">
            <Style.Resources>
            <LinearGradientBrush x:Key="BackBrush" EndPoint="0.5,1" StartPoint="0.5,0"  ColorInterpolationMode="ScRgbLinearInterpolation">
            <GradientStop Color="#FF8AA8D8" Offset="0.122" />
            <GradientStop Color="#FFCAD2DB" Offset="0.593" />
            </LinearGradientBrush>
            </Style.Resources>
            <Setter Property="Background" Value="{StaticResource BackBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            </Style>
            <Style x:Key="ColumnStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            </Style>
        -->
    </Application.Resources>
</Application>